# Neon Postgres connection string (use the "pooled connection" string from the Neon dashboard)
DATABASE_URL=""

# Long random string used to sign admin session cookies
SESSION_SECRET=""

# Bank transfer details shown to customers at checkout (manual confirmation, no gateway)
BANK_NAME=""
BANK_ACCOUNT_NAME=""
BANK_ACCOUNT_NUMBER=""

# Resend (dispatch/delivery notification emails) — get an API key at resend.com
RESEND_API_KEY=""
# Must be a verified sending domain, or use the shared onboarding@resend.dev for testing
RESEND_FROM_EMAIL="Rubyzatelier <onboarding@resend.dev>"
